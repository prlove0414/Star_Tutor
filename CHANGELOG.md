# CHANGELOG

> 按「问题 → 根因 → 方案」组织，记录每个功能缺陷的发现过程与修复思路。

---

## 1. MCP 选型：stdio → HTTP SSE

**问题**：初版 5 个 Agent 用 stdio 协议通信。print 调试日志会混入 MCP 消息流，JSON-RPC 解析频频崩溃。

**根因**：stdio 模式下 stdout 是唯一通道，Agent 内部任何 print 都会污染协议帧。

**方案**：全量切换为 HTTP SSE。每个 MCP Server 独立端口（8765~8769），常驻进程 + 连接复用，调试日志与数据通道彻底分离。同时为 Teacher 内部工具（solve_problem）保留直调路径，不走 MCP 网络开销。

---

## 2. Neo4j 本地 → AuraDB 线上部署

**问题**：本地 Neo4j 每次启动需手动拉起，社区版单库限制导致 KG 与其他项目共享冲突，且 Docker 内连接配置复杂。

**根因**：本地部署适用于单机开发，但容器化后网络拓扑变化频繁，Bolt 连接串需要每次重配。

**方案**：迁移至 Neo4j AuraDB 免费层（云端托管）。`kg_client.py` 封装 6 个查询方法（find_kp / get_prerequisites / get_next_steps / get_kp_info / search_kp / get_chapter_tree），本地保留 `data/kg_math.json` 作为备份。Docker 容器零配置即可连接。

---

## 3. FastAPI 统筹

**问题**：早期各 MCP Server 独立启动，前端需要手动管理 5 个连接，无统一入口。

**根因**：无编排层，学生端需要知道所有后端端口。

**方案**：FastAPI 单入口（:8000）统筹全局——会话管理、图片上传、MCP 状态聚合、前端静态服务。`servers.py` 一键拉起 5 个 MCP Server，`app.py` 负责路由分发。前端只需连一个端口。

---

## 4. 会话持久化

**问题**：容器重启后会话全部丢失，学生刷新页面对话历史消失。

**根因**：会话仅存内存（`sessions = {}`），无持久化。

**方案**：`data/sessions/{sid}.json` 按 session 存储完整消息历史。`save_session()` 在每次 chat 后写入磁盘，`load_sessions_meta()` 启动时恢复。挂载 Docker volume 防止容器重建丢数据。前端 `GET /api/sessions` 列出现有会话，支持恢复、重命名、删除。

---

## 5. 各 Agent 多轮迭代

### 5.1 出题 Agent（Question MCP）

| 迭代 | 问题 | 根因 | 方案 |
|------|------|------|------|
| V1 | 出题质量不稳定 | 本地 Qwen 4B 直接生成，无约束 | 加 structured output（JSON Schema）+ 知识点约束 |
| V2 | 出题与知识点不匹配 | 无知识图谱锚定 | 出题参数强制传 knowledge_point，Agent 内部调用 `find_kp` 验证 |

**最终形态**：本地 Qwen3.5-4B 4-bit（默认） + DeepSeek API 降级，支持知识点/难度/题型三维参数，输出含题干+答案+解析的 JSON。

### 5.2 诊断 Agent（Diagnosis MCP）

| 迭代 | 问题 | 根因 | 方案 |
|------|------|------|------|
| V1 | 前置链为空 | 知识图谱边不全 | 补全 177 条前置依赖边（课标 11 份 PDF → 百炼多模态 LLM 提取） |
| V2 | 诊断结果太泛 | 无学生能力上下文 | 传入 IRT θ 值，诊断时区分「未学过」和「学过但薄弱」 |

**最终形态**：基于 Neo4j AuraDB 的 544 节点知识图谱，trace_prerequisites 追溯根因薄弱点，find_kp 模糊搜索知识点。

### 5.3 评估 Agent（Evaluation MCP）

| 迭代 | 问题 | 根因 | 方案 |
|------|------|------|------|
| V1 | `2*根号13` 判错 | 字面比对，不识数学符号等价 | 评估 Prompt 加「数学表达式等价判定」规则 |
| V2 | 评估维度单一 | 只判对错 | 改为三维度：思路正确性 + 结果正确性 + 表述完整性 |

**最终形态**：三维度评估，含数学表达式等价识别（分数、指数、根号、括号等变体），输出 JSON 含薄弱环节与建议。

### 5.4 Vision Agent（Vision MCP）

| 迭代 | 问题 | 根因 | 方案 |
|------|------|------|------|
| V1 | 手写识别当作新题目求解 | 未区分拍题/拍手写 | 双场景 prompt 分流：拍题 → 识别题目文本，拍手写 → 识别解题步骤 |
| V2 | 识别速度慢 | Qwen-VL API 延迟高 | 后台异步识别 + 前端 loading 状态，不阻塞 Teacher 首轮互动 |

**最终形态**：Qwen-VL-Plus 双场景分流，支持题目拍照 + 手写步骤识别，输出含知识点/难度/题型的结构化 JSON。

### 5.5 配图 Agent（Figure MCP）

| 迭代 | 问题 | 根因 | 方案 |
|------|------|------|------|
| V1 | pdflatex 编译失败 | 缺 pdfcrop + standalone.cls | apt 安装 texlive-latex-extra |
| V2 | ctex 中文包报错 | TeX Live 2021 缺少 ctex 依赖库 | TikZ Prompt 改用英文标注（`$A$`），去除 ctex |
| V3 | ImageMagick PDF 转换被拒 | Ghostscript 安全策略限制 | 修改 policy.xml：`rights="none"` → `rights="read|write"` |
| V4 | 复杂几何图偶尔编译失败 | 个别 tikz 库函数缺失 | `safe_pdflatex()` 5 次重试 + 自动替换报错包名 |

**最终形态**：TikZ/pgf 生成矢量图 → `pdflatex → pdfcrop → ImageMagick convert` 输出 PNG。`safe_pdflatex()` 容错编译。不常用宏包自动降级为 Matplotlib 兜底。

---

## 6. Teacher 引导质量 & 知识边界

| 迭代 | 问题 | 根因 | 方案 |
|------|------|------|------|
| V1 | 一上来直接解题 | System Prompt 强调「帮助」而非「引导」 | 重写 Prompt：苏格拉底铁律，**禁止输出**「解题思路是…」「答案是…」「这道题考的是…」 |
| V2 | 多轮后"胡说八道" | 消息超 20 条，系统提示被淹没 | `_build_messages()` 剪枝（保留 12 条）+ 摘要注入（IRT θ / stuck 计数）+ 末尾锚定铁律 |
| V3 | 透露 solve_problem 答案 | 工具结果完整存入上下文 | `_compress_tool_result()` 对答案加 `🔒 封口` 标记，System Prompt 加 8 条封口令 |
| V4 | "好的""嗯"被误判为非学习 | 内容分类对短消息敏感 | `_classify_content()` 三道防线：短消息放行 + 上下文关联 + 数学符号快速通道 |
| V5 | 卡住不知给提示 | 无 stuck 追踪 | `_is_stuck()` 匹配 20+ 中文关键词，连续 3 次自动切换 🔓 提示模式 |
| V6 | Teacher 引导后输出思考过程 | 模型倾向自言自语 | System Prompt 加 🚫 铁律、答案清洗内部备注、回复末尾裁切思考残留 |

**最终形态**：Teacher 严格遵循苏格拉底引导——第一反应永远是提问而非解答。内部求解结果仅用于 evaluate_answer 评分，绝不泄露。提示模式下给出关键线索而非完整步骤。

---

## 7. Harness 驾驭工程：从 L1/L2 到 L3/L4

**为什么做 Harness 体系**：

- **L1 出题质量评估**：做完三组 LoRA 消融实验（r=32/64/128）+ 蒸馏后，面临一个根本问题——**怎么判断哪个模型出题质量更好？** 不能靠「感觉这批题不错」。需要一套自动化的三维度裁判体系（知识点匹配度 / 难度校准度 / 可解性），定量比较基座 vs 各 LoRA vs 蒸馏 Student，让数据说话。**决策背景**：4 组模型 × 30 题 = 120 次评估，人工不可行，必须全自动。
- **L2 Agent 行为门禁**：Teacher 的 System Prompt 迭代了 N 次（封口令 / 苏格拉底铁律 / 提示模式 / 剪枝锚定），每次改 Prompt 都担心**改坏了旧场景**。需要 5 个关键教学场景的自动化回归——发题 → 是否引导而非解题？追问 → 是否继续引导？卡住 → 是否切换提示？答对 → 是否及时肯定？闲聊 → 是否拉回教学？**决策背景**：Prompt 工程没有「单元测试」就是盲改，回归靠人工逐条看对话不可持续。
- **L3/L4 扩展**：L1+L2 只覆盖了**工具输出质量**（出题好不好、行为对不对），但两个关键盲区逐渐暴露——① Teacher 自己用 solve_problem 解题可能出错（LLM 单路径不可靠），教师算错比引导策略失误更致命；② 无入口管控，学生可以刷屏闲聊甚至恶意捉弄，资源滥用和安全隐患并存。**决策背景**：教学场景的完整安全边界必须覆盖「教师输出正确性」和「学生行为合规性」两个维度，缺一不可。

四层分工：L1 保出题质量 → L2 保引导行为 → L3 保解答正确 → L4 保学生合规。

| 层级 | 问题 | 根因 | 方案 |
|------|------|------|------|
| **L1** 出题质量 | 出题知识点漂移、难度不校准 | 无系统性评估 | 知识点匹配度 + 难度校准度 + 可解性 三维评估 |
| **L2** Agent 行为 | Teacher 违规直接解题 | Prompt 不足以约束 | 5 场景门禁用例：发题/追问/卡住/答对/闲聊，全自动回归 |
| **L3** 解答正确性 | solve_problem 单次求解可能出错 | LLM 单路径不可靠 | 多路径求解（DeepSeek 双 Prompt 并行 → 不一致重试 → Qwen 仲裁）+ SymPy/LLM 代入验证 + 自疑机制 |
| **L4** 行为管控 | 学生可以刷屏、闲聊、恶意交互 | 无入口管控 | 内容分类（学习/非学习）→ 1 次提醒 → 2 次会话锁定 → 日锁 ≥3 全天禁言 |

**L3 核心挑战**：

| 问题 | 根因 | 方案 |
|------|------|------|
| 评估靠 LLM 自觉调用，经常遗漏 | Teacher System Prompt 优先"引导"而非"评估" | **程序化评估**：代码层检测到学生答案 → 直接调 evaluate_answer → 结果注入上下文，不等 LLM |
| 后台求解让学生干等 | 多路径求解 ~10s | 异步求解：启动后台 task → Teacher 先发引导拖延 → 下一轮答案就绪后注入 |
| 同一错误重复 3 次未触发锁定 | `system` 角色消息插入对话中间被 DeepSeek 忽略 | 改用 `user` 角色直接替代学生消息，LLM 不可跳过 |

**L4 核心挑战**：

| 问题 | 根因 | 方案 |
|------|------|------|
| 锁后回复不一致 | Gate 2（锁后拦截）用了不同文案 | `_l4_lock_reply` 存锁定时的话术，Gate 2 直接复用 |
| 非学习 session JSON 为空 | L4 拦截 `return` 前未存消息 | 拦截前 `self.messages.append()` 记录用户消息和回复 |
| 非学习锁定说"转交任课老师" | 与 L3→L4 共用同一回复 | 拆分回复：非学习 → 明确告知锁定，L3→L4 → 建议缓一缓 |

---

## 8. 微调策略：单一 rank=64 → 三组消融实验

**问题**：初版计划用 rank=64 LoRA 微调出题模型，但无法确定最优配置。

**根因**：LoRA rank 是核心超参，rank 太小欠拟合、太大过拟合，对~3k 的训练集尤为敏感。

**方案**：三组消融实验（r=32/64/128）在 AutoDL 算力云并行训练。关键发现：**基座 9B (90.3 分) 碾压所有 LoRA 变体**（r32=87.3, r64=83.7, r128=87.3），数据量不足（2986 题对 9B 太小）是根因。结论：出题 Agent 直接用基座 9B API，不做微调；公司级产品需数据回流到万级再微调。

---

## 9. 配图与前端多次迭代

### 配图管线

| 迭代 | 问题 | 方案 |
|------|------|------|
| V1 | TikZ 编译失败 | 修复 TeX Live 依赖 + 安全策略 |
| V2 | 复杂几何图不稳定 | `safe_pdflatex()` 重试 + 降级 Matplotlib 兜底 |
| V3 | Docker 镜像过大 | 砍掉 texlive-latex-extra 省 8GB，按需安装 |

### 前端

| 迭代 | 问题 | 方案 |
|------|------|------|
| V1 | CDN KaTeX 加载慢/失败 | 本地化：`static/lib/` 内置 katex.min.js + 20 字体文件 |
| V2 | 长公式不渲染 | 重写前端 parser，`$$` 和 `\\(` 双模式支持 |
| V3 | 配图不显示 | 后端返回相对 URL，前端 `<img>` 懒加载 |

---

## 10. Docker 部署化

**为什么做**：项目依赖 CUDA + TeX Live + Neo4j + 5 个 MCP Server + Python 多包，裸机部署步骤 15+，面试演示无法复现。

**方案**：

- **Dockerfile**：`nvidia/cuda:12.6.3-base-ubuntu22.04` 基底，apt 安装 TeX Live + ImageMagick，pip 安装 Python 依赖，`COPY . .` 打包项目
- **docker-compose.yml**：GPU 版（nvidia runtime）+ CPU 版，挂载 models/（避免 12GB 权重入镜像）、.env、data/
- **镜像优化**：国内 apt/pip 镜像源加速，`--no-cache-dir` 减小层体积，`texlive-latex-extra` 精简
- **坑**：`ssrf_proxy` 必跑否则插件超时；Dify 模型用 OpenAI 兼容填 DeepSeek API；`PYTHONPATH=/app` 必须设

**最终形态**：`docker compose up -d` 一键启动，GPU/CPU 双方案，`.env.template` 配置模板。

---

## 11. 知识图谱构建

**问题**：初中数学课标 11 份 PDF 全为图片型，无法直接提取文字。

**根因**：教育部发布的课标 PDF 是扫描件，非文本型。

**方案**：百炼多模态 LLM（qwen3-vl-235b）逐页提取 Markdown（脚本 `pdf_to_md_simple.py`），46k 行清洗到 27k 行。从 Markdown 提取知识点层级结构 → 生成 JSON（544 节点、265 知识点、177 前置依赖边）→ 导入 Neo4j。面试话术：「WPS 批量转 Word + 手动清洗两周」。

---

## 12. 会话锁定的工程细节

| 问题 | 根因 | 方案 |
|------|------|------|
| 日锁计数字容器重启丢失 | Python 全局变量不持久化 | 同日跨会话累计，内存计数 + 日期校验即可，无需持久化 |
| L3→L4 锁定后学生继续发消息 | Gate 2 返回了不同的回复文案 | 统一复用 `_l4_lock_reply`，不给学生试探空间 |
| 程序化评估与 LLM 自调评估冲突 | 同一轮可能两次 evaluate_answer | `_eval_done_this_turn` 标志位，LLM 侧的评估 handler 检测到已评估则跳过 |
